name: Deploy Earth

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main
        with:
          ref: ${{ github.ref }}

      - run: |
          ./deploy-earth.ps1
        shell: pwsh
